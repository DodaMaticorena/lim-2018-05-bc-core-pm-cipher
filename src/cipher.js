// window.cipher = {
  // ... 
// };

function encode(texto,numeroDesplazamiento) {
  let textoCifrar = []
  for (let a = 0; a < texto.length; a++){
    const letras = "ABC"
  }
}
if (inputWord.match(/[A-Za-Z/])
const codificar = (numeroDesplazamiento, texto) => {
//  return 'hola';  
  let 

}

const decodificar = (numeroDesplazamiento, texto) => {
//  return 'chau';  
  let 

}

window.cipher = {
  encode: codificar,
  decode: decodificar
}